apiVersion: v1
kind: Service
metadata:
  name: nkp-academy
  namespace: nkp-academy
  labels:
    app.kubernetes.io/name: nkp-academy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nkp-academy
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
