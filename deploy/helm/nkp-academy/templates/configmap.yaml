apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nkp-academy.fullname" . }}-config
  labels:
    {{- include "nkp-academy.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
