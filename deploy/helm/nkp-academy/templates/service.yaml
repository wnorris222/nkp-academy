apiVersion: v1
kind: Service
metadata:
  name: {{ include "nkp-academy.fullname" . }}
  labels:
    {{- include "nkp-academy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nkp-academy.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
